{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "onescm.serviceAccountName" . }}
  labels:
    {{- include "onescm.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
